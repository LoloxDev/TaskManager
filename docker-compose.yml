version: '3'
services:
  mon_postgres:
    image: postgres
    container_name: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    volumes:
      - /mnt/wsl/Ubuntu-20.04/home/lolo/Projects/TaskManager/data/postgres:/var/lib/postgresql/data
